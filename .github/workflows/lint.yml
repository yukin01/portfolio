name: Lint And Build
on:
  pull_request:
    branchs:
      - '*'

jobs:
  build:
    name: Greeting
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - run: echo 'Hello World!'
